import React from "react";
import Pending_Work from "../../common/pending-works/Pending_Work";

const My_Returns = ({ name }) => {
  return (
    <>
      <Pending_Work name={name} />
    </>
  );
};

export default My_Returns;
